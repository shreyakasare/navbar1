import React, { useContext } from 'react'
import USeCntxt from './Cntxt/USeCntxt';

const Dtl = () => {
    const { uName } = useContext(USeCntxt)

    return (
        <>
            <div>Detail</div>
            {/* <button onClick={() =>}></button> */}
        </>
    )
}